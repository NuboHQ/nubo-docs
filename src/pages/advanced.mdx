import { Tabs, Tab } from '@/components/Tabs';

# Advanced

## Install

<Tabs items={['npm','pnpm', 'yarn']}>
  <Tab>
    ```bash copy
    npm install nubo
    ```
  </Tab>
  <Tab>
    ```bash copy
    pnpm add nubo
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn add nubo
    ```
  </Tab>
</Tabs>
